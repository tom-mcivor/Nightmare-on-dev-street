<div class="container">
  {{#each horrorMovies}}
  <div class="movie-list">
    <a href="/movie/{{id}}" style="text-decoration:none"><img class="img-circle" src="{{image}}" alt="{{movie}}"><h2>{{movie}}</h2></a>
    
  </div>
  {{/each}}
</div>

