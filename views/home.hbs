<div class="container">
  {{#each horrorMovies}}
  <div class="movie-list">
    <a href="/movie/{{id}}"><img class="img-circle" src="{{image}}" alt="{{movie}}"></a>
    <h2>{{movie}}
  </div>
  {{/each}}
</div>
